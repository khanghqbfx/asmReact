import classes from "./Header.module.css" 


const HeaderDeital = () => {
    return(
        <div className={classes.header}>
            <span className={classes.big}>Detail</span>
            <span className={classes.small}>Detail</span>
        </div>
    )
}

 export default HeaderDeital